# WorldView

WorldView is a webpage for aggregating how news pages appear across the world. You can view it at insert link here

## Development
To develop for WorldView first run `make install_hooks` this will set up automated formatting and evaluation for your code.
To run tests run `make test`
